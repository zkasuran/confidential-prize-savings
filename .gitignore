# Build workspace artifacts
node_modules/
dist/
coverage/
coverage.json

# Hardhat
hardhat/artifacts/
hardhat/cache/
hardhat/fhevmTemp/
hardhat/types/

# Secrets / env (addresses are public and live in source; keys never are)
.env
.env.*
!.env.example

# Local-only notes and the large reference clone (not part of the deliverable)
LEDGER.md
ref-dapps/

# Editor / OS
.DS_Store
*.log

# TS build info
*.tsbuildinfo

# Built site deploy dir (separate public repo)
site-deploy/

# Internal prep (not part of the public deliverable)
SUBMIT-PACKET.md
content/
semantic-review/

# Internal working material and video build outputs. This repository is public,
# and none of this is part of the deliverable.
RECORD-VIDEO.md
submit/
upload/
DEMO-*.mp4
deck-shots/
frames/
cues/
